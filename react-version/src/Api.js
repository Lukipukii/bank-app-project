const SERVER_URL = 'http://localhost:3000'
const LOGIN_URL = `${SERVER_URL}/login`
const USER_URL = `${SERVER_URL}/user`
const TRANSFER_URL = `${SERVER_URL}/transfer`
const MOVEMENTS_URL = `${SERVER_URL}/movemvent`

const API = {
  CATEGORIES_URL,
  DISHES_URL,
  RESTAURANTS_URL,
}

export default API
